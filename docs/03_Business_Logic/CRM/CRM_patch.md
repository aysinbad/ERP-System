# CRM — Pricing Integration Patch
# هذا الملف يحتوي على البنود التي تُضاف لـ CRM.md فقط
# أضف كل قسم في المكان المحدد

---

## الإضافة 1: تحديث Business Rule #3 (crmOppToProforma)

**ابحث عن نهاية وصف Business Rule #3 (تحويل الفرصة→بروفورما) وأضف:**

```markdown
> **تحديث (2026-07-21 — ميزة السعر الاسترشادي):** قبل تنفيذ التحويل، يُتاح للمستخدم اختيار سعر البند من السعر الاسترشادي المحسوب (بصلاحية `pricing.viewSuggestedPrice`) أو سعر يدوي. السعر المختار يُحفَظ كـ Snapshot على البروفورما — لا تُعيد البروفورما حسابه لاحقاً. الفجوة القديمة (`Product.price` الثابت) محسومة بهذا القرار.
```

---

## الإضافة 2: Business Rules #12 و #13 (أضفهما قبل قسم Business Invariants)

```markdown
### 12. رؤية السعر الاسترشادي في CRM (Pricing Visibility)

> هذه القاعدة تحدد ما يرى / ما لا يرى مستخدم CRM من بيانات التسعير. التفاصيل التقنية في `CRM_Implementation_Guide.md` قسم Pricing Integration.

مستخدم CRM بصلاحية `pricing.viewSuggestedPrice` يرى **فقط:**
- ✅ السعر الاسترشادي النهائي للمنتج.
- ✅ حالة الموثوقية: "محدَّث" أو "يحتاج مراجعة الإدارة".
- ✅ تنبيه مبسَّط عند وجود بيانات قديمة: *"السعر الاسترشادي مبني جزئياً على بيانات تجاوزت 3 أشهر تقريباً ويحتاج مراجعة الإدارة."*

مستخدم CRM **لا يرى في أي حال:**
- ❌ تكلفة المنتج أو الخامات أو الهدر.
- ❌ هامش الربح أو نسبته أو قيمة الربح.
- ❌ نصيب المصاريف العامة أو تفاصيل بنود التكلفة.
- ❌ أي تعديل يدوي على التكلفة (Cost Override) أجرته الإدارة — يرى السعر الناتج بعده فقط.
- ❌ معادلة الحساب أو تفاصيل محرك التسعير.
- ❌ تفاصيل البنود القديمة (أي بند، تاريخه، قيمته) — التنبيه العام فقط.

مستخدم CRM بلا صلاحية `viewSuggestedPrice` أو `viewCost`: **لا يرى أي معلومات تسعير**.

### 13. السعر الاسترشادي كمصدر اختياري للبروفورما (Price Snapshot)

عند تحويل فرصة ناجحة لبروفورما، يستطيع المستخدم (بصلاحية `pricing.viewSuggestedPrice`) اختيار سعر البند من:
- السعر الاسترشادي المحسوب.
- سعر يدوي (يخضع لفحص الاستثناء إذا كان خارج السياسة).

**قواعد الاختيار:**
1. إذا كانت بيانات التكلفة قديمة (>90 يوم): يُعرَض تنبيه، ويتطلب الاستمرار صلاحية `pricing.approvePriceException` + سبب إجباري.
2. السعر المختار يُحفَظ كـ **Snapshot ثابت** على البروفورما (السعر + حالة الموثوقية + من اختاره + متى).
3. **البروفورما لا تُعيد حساب السعر لاحقاً** من محرك التسعير — ما حُفِظ وقت التحويل يبقى ثابتاً.
4. إذا اختار المستخدم سعراً يدوياً بعيداً عن السعر الاسترشادي: يُطبَّق فحص الاستثناء (`approvePriceException`) إذا كان خارج نطاق السياسة.

**⚠️ ملاحظة الـ Prototype:** `crmOppToProforma` في الـ Prototype يستخدم `Product.price` الثابت. هذه القاعدة (#13) تحل محله في البناء الإنتاجي.
```
